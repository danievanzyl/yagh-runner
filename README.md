# yagh-runner

```bash

> docker run -rm -e REPO=<name_of_repo> -e REG_TOKEN=<runner_token_under_repo> \
-e NAME=<name_of_runner> \
-v /var/run/docker.sock:/var/run/docker.sock \
name_of_image:latest

```

